# Transfer Staging

Use this folder for files that should move between the MacBook Pro and the
Raspberry Pi.

- Put Mac-to-Pi payloads in `transfer/mac_to_pi/`
- Pull Pi-to-Mac payloads into `transfer/pi_to_mac/`

Payload files are ignored by Git. The folders are kept with `.gitkeep` files.

